/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import StyleSheet from './StyleSheet';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => StyleSheet);
